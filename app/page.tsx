/**
 * Placeholder de PHASE 02 — vérifie uniquement que la stack tourne.
 * La homepage réelle (Hero → Social Proof → Services → … → Footer,
 * ordre imposé par le PROMPT §12) est construite en PHASE 07.
 */
export default function HomePage() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-4 bg-deep px-6 text-center">
      <p className="text-xs uppercase tracking-[0.3em] text-light">
        Adrar Media — Digital Agency
      </p>
      <h1 className="text-3xl font-semibold uppercase tracking-tight text-white sm:text-5xl">
        From Local to Global.
      </h1>
      <p className="max-w-md text-sm text-beige">
        Setup terminé. Homepage en attente de la PHASE 07.
      </p>
    </main>
  );
}
